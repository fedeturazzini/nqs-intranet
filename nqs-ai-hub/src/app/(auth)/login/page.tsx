/**
 * Placeholder — la pantalla de login real se construye en
 * `prompts/mvp/03-auth.md`. Acá sólo dejamos la ruta declarada
 * para que `(auth)` exista como segment group del App Router.
 */
export default function LoginPage() {
  return (
    <main className="center grow" style={{ padding: 80 }}>
      <div className="t-eyebrow">auth · login</div>
    </main>
  );
}
